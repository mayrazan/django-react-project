import styled from "styled-components";

import { colors } from "../../../styles/colors";

export const ContainerManagerStyled = styled.div`
  background-color: ${colors.white};
  width: 100%;
`;

export const HeaderFooterManagerContainer = styled.div`
  padding: 1rem;
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  justify-content: space-between;
`;
